# Los archivos subidos por usuarios viven en storage/uploads/ (fuera del web root)
# y los ignora storage/uploads/.gitignore. Ya no existe public/uploads: se eliminó
# al mover las evidencias de talleres a storage/ (2026-08-27).

# Entorno / dependencias locales
/vendor/
.env
*.log

# Configuración con credenciales — NO versionar (usar config/config.example.php)
config/config.php

# Artefactos del sistema operativo / editores
Thumbs.db
.DS_Store
.idea/
.vscode/
